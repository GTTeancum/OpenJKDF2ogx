# Bot lift and four-player performance work — 2026-09-14

**FIXED / accepted - 2026-09-14.** The user accepted approximately 40 FPS for active four-player/eight-bot gameplay and explicitly closed the performance item. This supersedes the earlier pursuit of consistent 60 FPS and further performance follow-ups below. The reproduced lift-contact and round-trip fix was already verified; the elevators/FPS work is closed. Routing remains separate and untouched, and the consolidated hardware run remains a later task. Accepted results are removed from `TO_DO.MD`; historical measurements and their limitations are retained here.

Initial read-only assessment (Extra High confirmed):
- Lift attachment records the passenger's existing position and sets NO_MOVE before ordinary standing-height correction. A proximity fallback also attaches without a collision face. This can lock a bot inside the 3DO indefinitely. Require actual upward-facing contact and actor standing clearance before locking the attachment; remove proximity-only boarding/reacquisition.
- Historical PerfSplit evidence places rendering above all simulation. Current active baseline uses four human viewports, eight bots, movement/primary fire, authored-spawn traversal, 16:9 and normal sound. Do not use idle screenshots as the performance baseline.
- Current baseline sample: roughly 35–40 FPS during combat. Aligned windows attribute about 6.3 seconds to world rendering, 3.3–3.6 seconds to simulation (1.5–1.7 seconds bot AI), and 1.9–2.0 seconds of world time to submission. Counters are inclusive; do not add nested stages or assume a ten-second CPU span.
- Bot floor and nearest-node collision queries dominate bot time; rendering/model and submission work still cap overall gains. Avoid changing AI frequency or suppressing safety checks. Reuse identical scenarios and compare several steady windows; randomized combat alone cannot establish a precise percentage improvement.

Validation plan: existing directed m2 lift goal, native third-person capture sequence, explicit top-contact/standing-height logs across travel and exit; active four-player/eight-bot baseline and after-run with frame and clock summaries. No host input, asset edits, or unrelated changes.

First implementation/results:
- Boarding now requires a real upward-facing lift face. Resolve the face plane plus the actor's existing standing clearance before recording a NO_MOVE attachment; reject distant/invalid contacts. Removed proximity-only boarding and moving-car reattachment.
- Probe-only camera shows foot contact; normal gameplay camera settings are unchanged. The delayed round-trip goal fixture uses ordinary bot navigation and level lift controls, with no runtime position warps.
- Maps without authored controlled traps now skip their hazard route segment traces and the enemy nearest-node lookup used only for that check. 600 production-function regression cases preserve behavior; nonempty hazard sets run unchanged. Standing-height, lift landing and sector containment tests pass.
- Baseline 20260914_193029-bots-4p-active-baseline: 36.16 measured host FPS over 77.43 seconds, game/host ratio 0.99981–0.99996, physics/host ratio 0.99976–0.99991.
- After 20260914_194341-bots-4p-active-after: 37.30 measured host FPS over 71.90 seconds, game/host ratio 0.99936–0.99953, physics/host ratio 0.99936–0.99953. Same map, four locals, eight bots, primary fire and authored-spawn traversal. All three saved native captures from each run were inspected: active players/bots, weapons and world/HUD rendering are present.
- Bot stage windows fell from roughly 1.5–1.7 seconds to 1.0–1.2 seconds; world rendering remains roughly 6.5–7.0 seconds in aligned counter windows. Combat is randomized, so the approximately 3% whole-run FPS difference is observational, not an isolated speedup guarantee. Consistent near-60 FPS remains open.
- Latest upward contact captures: 20260914_194606-bots-lift-feet-proof, frames 0–5 inspected sequentially, plus 10/20/30. Feet remain atop the lift through the visible final ascent, then the bot exits onto the upper floor. Probe camera can leave portal bounds while following the platform, making world surroundings black in some lift frames; this is not accepted as normal-camera/world-rendering qualification. Earlier 20260914_193932-bots-lift-boarding-proof frames 16–27 were inspected sequentially for approach, ascent and exit, but the original framing cropped feet.

Round-trip verification:
- Run `20260914_194945-bots-lift-roundtrip` completed 77 seconds, 11 successful polls, no fatal reports. The JK1MP `m2.jkl` fixture directs lower node 4 -> upper node 144 -> lower node 4. All three arrivals are recorded in the final RAM log, with actual platform boarding, switch activation, movement, and floor disembarkation between them.
- Inspected native capture frames 34–60 sequentially: approach/boarding 35–38, ascent 39–43, upper exit 44, return boarding 45–46, wait/descent 47–53, lower exit and continued routing 54–60. Also inspected startup/wait frames 0/10/20/30. This is a review of those saved captures, not every rendered game frame or all 120 saved images. The widened probe camera clips near walls and sometimes leaves portal bounds; feet are visible through most of the ride, with temporary occlusion in frames 41 and 51–52.
- Both boarding contacts use the actual 3DO face plane: lower surface Z=0.7998, standing Z=0.9199; upper surface Z=1.2998, standing Z=1.4199. Actor clearance is 0.1201. Passenger/lift relative Z remains 0.1338 throughout both directions, and the bot reaches the correct world floor afterward. These values and the inspected images establish top contact rather than merely route-node arrival.
- Added a rejection for `GetSectorLookAt` shortening the height correction against a ceiling, with a regression case proving no position mutation or attachment is attempted. The final lift build succeeded; contact, landing-height, sector containment, route-floor, trace-routing and empty-hazard tests pass. Normal game camera rendering was inspected in the separate four-view combat captures.
- The reported embedding/round-trip problem is fixed for this reproduced lift. Other maps, contested multi-bot boarding, and CTF route stalls are not established by this single directed fixture. CTF remains open; no movement-speed boost was introduced.

Additional bounded rendering change:
- Xbox vertex lighting previously called `Len3`, then `Normalize3Acc` on the same vector, repeating the distance square root. Reuse that exact length with the same component divisions and zero-vector behavior. Light selection, falloff comparison, intensity calculation, ordering and the MotS directional branch are unchanged.
- `test_vertex_light_length.py` compiles the production function with Xbox optimization and its non-Xbox reference branch. 24,000 cases across both lighting configurations produce bit-identical outputs, including zero distance, falloff boundaries, saturation, zero vertices/lights and MotS compatibility. They remove 112,077 and 86,219 redundant square roots respectively; the MotS directional branch retains its original call count.
- The user's own XEMU process was running during the source follow-up and was left untouched. It had exited before the first process inventory of the final runtime test; subsequent inventories showed only the test-owned emulator, which the harness closed normally.
- Build `bot_light_length_build_log.txt` succeeded. Final run `20260914_200111-bots-light-length-four-visual` completed 76 seconds with six successful polls and no fatal reports. All three unique native captures (29/52/74 seconds) were inspected in order: four viewports, lit models/world geometry, HUDs, weapons, traversal and shot-triggered forcefield translucency remain visible. Several cameras temporarily face nearby walls during the traversal. This is not an audible-output test or a frame-by-frame flicker test.
- The final clock summary measured 40.89–40.90 host FPS over 33.54–33.56 sampled seconds, game/host ratio 0.99898–0.99934 and physics/host ratio 0.99892–0.99928. This is a shorter randomized active-play workload than the 36.16 FPS initial baseline and the 37.30 FPS intermediate run; it supports continued improvement, not an isolated percentage guarantee. Consistent 60 FPS and remaining CTF routing stay open.

Deployment at that checkpoint: copied the verified release to `C:/Games/Emulators/CXBX/openJKDF2x/default.xbe`, SHA-256 `CF4E12691AB5B5FD2E4AF24300838C1C60ECF9F45294E6B94469BDCB0F64561A`. Test-owned XEMU exited; no host input or user-session termination was used.

Scope correction: the active goal covers elevators and FPS only. Existing routing changes remain in place, but no further routing work is authorized in this goal. CTF is a separate unfinished item.

Bulk vertex submission candidate:
- Textured triangle batches now cross the C/C++ renderer interface once per batch, instead of once per vertex. The shared 40-byte vertex layout, triangle order, projection, lighting/color conversion, UV state and 768-vertex draw boundaries are preserved. The immediate-mode fallback remains available. No player/bot count, simulation rate, detail level or AI frequency was reduced.
- `test_lit_batch.py` compares the actual production scalar and bulk paths byte-for-byte, including both projections, all alpha bytes, out-of-range light values, batch boundaries, repeated vertices, multiple submissions, retained UV/color state and immediate-mode fallback. It passes. `test_lit_vertex_submission.py` also passes 131,072 combinations. Release build `fps_bulk_submit_build_log.txt` succeeded.
- Fresh baseline `20260914_201539-fps-submit-baseline` measured 39.58–39.59 host FPS over 80.68–80.69 sampled seconds. Game/host ratio was 1.00001–1.00016; physics/host was 1.00011–1.00026. Both unique native captures (39/87 seconds) were inspected: four world/HUD views, weapons, traversal and visible forcefield translucency.
- Candidate run `20260914_202414-fps-submit-after` completed 106 seconds, six successful polls and no fatal reports. Both unique native captures (39/88 seconds) were inspected in order: four correctly projected world/HUD views, textured weapons and translucent forcefield rendering. Several cameras face close walls; these still captures do not establish absence of intermittent flicker or full bot-model animation correctness.
- A separate OpenRedFaction XEMU process started during candidate startup and remained alive through the test. It was left untouched. Candidate host FPS was 33.82–33.83 over 65.30–65.32 sampled seconds, with game/host 0.99905–0.99927 and physics/host 0.99882–0.99904. **This is not an uncontended before/after comparison and does not establish an overall speedup or regression.**
- Across saved PerfSplit windows, frame-weighted submission cost was 5.552 ms/frame baseline versus 4.027 candidate; world rendering was 15.892 versus 15.896, and simulation was 5.905 versus 9.597. These are inclusive counters from randomized workloads under different host contention. The reduced submission cost supports the targeted change, but a clean active-play comparison is still required before claiming an FPS improvement.
- Candidate release SHA-256: `586E74A659AA9774916BA0639C78E3B5A6A299EC6897DDCC8AC9C304CE35DA7B`. It remains in `build/xbox/release/default.xbe`; the normal runtime has not been replaced with this provisional candidate. Test-owned XEMU exited normally. Consistent near-60 FPS remains open.

Uncontended follow-up and renderer profiling:
- `20260914_202923-fps-submit-uncontended` completed 121 seconds, seven good polls, no fatal reports. It measured 39.76–39.77 host FPS over 81.02–81.04 sampled seconds, versus the earlier 39.58–39.59 baseline. This does not establish a meaningful whole-game gain. Frame-weighted inclusive costs: world 15.913 ms, simulation 5.967 ms, submission 4.948 ms.
- The buffered grayscale path now performs one grayscale float-to-integer conversion and replicates the byte to RGB, instead of converting the identical value three times. Opacity remains separately converted; the immediate fallback is unchanged. The production-path regression now uses the real identity `Clamp` macro and covers byte-color/nongray transitions, retained state, boundaries and fallback; it passes. `20260914_203444-fps-scalar-color` measured 39.75 FPS over 80.85–80.86 sampled seconds, with no established net FPS gain.
- Enabled SSE1 only for `fakeglx`, `std3D`, `rdCache`, `rdClip`, `rdPrimit3` and `rdModel3` in the Xbox build. Physics, bot and simulation compilation flags are unchanged. Local XDK compilation and disassembly verify SSE1 emission, without SSE2. `20260914_203850-fps-renderer-sse` completed 121 seconds, seven good polls, no fatal reports; 41.37–41.38 FPS over 81.25–81.26 sampled seconds. Simulation cost differed across randomized combat, so this is not an isolated SSE percentage gain.
- All three unique native captures from each of those runs were inspected chronologically (38/70/103, 38/71/103 and 38/71/103 seconds respectively). World lighting, HUDs, weapon views, traversal and forcefield translucency remain visible; later runs include visible bot models. These are saved stills, not exhaustive animation/flicker or audible-output qualification. Game and physics rates remain approximately 1:1 with host time.
- Opt-in `-bot-profile` now samples model transform, lighting and face work every eighth rendered frame, across every local view. `20260914_204436-fps-model-profile` showed face processing dominating those sampled stages (roughly two thirds), followed by lighting and transforms. Face counters include any nested submission; do not add them to inclusive PerfSplit totals. Another project's emulator overlapped that run, so it is profiling direction rather than FPS qualification. Both native captures (39/72 seconds) were inspected. Ordinary gameplay does not execute these new timing calls without the flag.

User-requested pillarbox experiment:
- On a 16:9 system, 3P/4P now use a centered physical 4:3 canvas with black outer sidebars. Native view rectangles shrink from 320x240 to 240x240; anamorphic display makes each a physical 4:3 view. Gameplay pixel coverage falls 25%. The camera and GPU use matching projection pixel aspects, preserving vertical FOV and reducing horizontal coverage. HUD proportions and anchoring use the physical display aspect. 1P/2P and 4:3 system layouts remain unchanged.
- Test-only `-split-fullwidth` selects the old wide layout in the same binary. `test_split_pillarbox.py` passes production-helper geometry/projection checks for 1–4 players, both system aspects, both modes, and exiting split-screen. Release build `fps_pillarbox_ab_build_log.txt` succeeded. Trial XBE SHA-256: `2C84252482BA614D79B6881B10CDD4D4E22685921846E7F0B6C53F6DA50569D8`.
- `20260914_205416-fps-4p-pillarbox`: 107 seconds, six good polls, no fatal reports; 39.195–39.203 host FPS over 65.123–65.135 sampled seconds. Native captures 40/72/105 seconds inspected in order: four correctly proportioned world/HUD views, black sidebars, weapon rendering, active fire and forcefield translucency.
- `20260914_205603-fps-4p-fullwidth`: 120 seconds, seven good polls, no fatal reports. A separate OpenRedFaction XEMU started at 20:58:00; discard the seventh poll ending 20:58:02 for comparison. The first six polls end at 20:57:45 and provide 64.806–64.821 uncontended sampled seconds at **40.712–40.721 FPS**. Including the contended seventh poll produces 39.04–39.05 FPS and would be misleading. Native captures 37/69/102 seconds inspected in order: four wide world/HUD views, weapons, visible bot in the middle capture and forcefield translucency.
- Both clean 4P samples have game/host and physics/host ratios approximately 1.0. Pillarboxing has **not established an FPS improvement**; the 1.5 FPS observed decrease is also not an isolated regression measurement because combat is randomized. Reduced pixel coverage does not remove the CPU work of rendering four views and simulating eight bots.
- The first 3P visual run, `20260914_210354-fps-3p-pillarbox`, began while the other emulator was still present. Both native captures (40/74 seconds) were inspected, but its 13.74 FPS aggregate is excluded from performance qualification. The other process was gone before the clean pair below started and absent at their observed midpoint/end inventories; it was never controlled or terminated by this task.
- Clean 3P full-width run `20260914_210550-fps-3p-fullwidth-clean`: 96 seconds, five good polls, no fatal reports; **46.542–46.554 FPS** over 48.546–48.558 sampled seconds. Inspected all three native captures chronologically: 29 seconds shows the loading screen; 61/94 seconds show three active world/HUD/weapon views, the unused black quadrant, and visible player/bot models. Loading time is excluded from the valid gameplay clock sample.
- Clean 3P pillarbox run `20260914_210747-fps-3p-pillarbox-clean`: 89 seconds, five good polls, no fatal reports; **50.899–50.913 FPS** over 48.946–48.959 sampled seconds. Both native captures (38/71 seconds) were inspected in order: correct three-view pillarboxing, HUD anchors, world/weapon rendering, black unused quadrant, and a visible nearby character in P3 at 71 seconds. These tests exercise movement and primary fire with eight bots; they are not idle baselines.
- The 3P observation is approximately **+4.4 FPS / +9.4%**, with game/host and physics/host ratios approximately 1.0 in both layouts. Randomized combat means this is an observed workload difference, not a guaranteed isolated gain. Frame-weighted inclusive 3P world cost fell from 11.307 to 10.898 ms/frame and submission from 3.879 to 3.420; simulation was 6.012 versus 5.917. The corresponding clean 4P world costs were 14.522 full width versus 14.435 pillarboxed, and submission 4.152 versus 4.190, supporting the absence of a meaningful 4P benefit in this trial.
- Comparison data are saved under `build/xbox/issue1_validation/xemu_smoke_runs/pillarbox_3p_comparison.json` and `pillarbox_4p_comparison.json`. All capture paths above are under the matching run's `screenshots` folder. Test-owned XEMU exited normally. No routing edits were made for this experiment.
- The normal runtime XBE remains the earlier deployed `CF4E12691AB5B5FD2E4AF24300838C1C60ECF9F45294E6B94469BDCB0F64561A` checkpoint; the pillarbox trial is currently in the release/test build. Retain the trial for review; do not treat it as a resolution of the 60 FPS goal.

Xbox face-cache depth-only candidate:
- Following the measured model-face cost, source inspection found `rdCache_AddProcFace` still calculating software screen bounds, edge indices, four ceiling conversions, and aggregate damage extents for every submitted face. Repository consumers of those fields are confined to the software bookkeeping in `rdCache.c`; Xbox builds with `SDL2_RENDER`, selects the hardware renderer, and skips software rectangle rejection.
- Added an Xbox/SDL2-only path retaining view-space Y depth extrema for hardware mip selection and painter sorting, face metadata, colormap and vertex/UV/light buffer accounting. Unused software fields are initialized to zero. The other platform paths remain unchanged. No faces, lights, bots, views or simulation work are suppressed.
- `test_face_depth_bounds.py` compiles the actual production function and its retained reference branch. 40,000 cases with 3–32 vertices cover cache capacity, all flag bytes, repeated and negative depths, signed zero, metadata and untouched cache slots. Every consumed field and allocation counter matches byte-for-byte; ceiling call count falls from four to zero for each accepted face. The software-only bounds/edge fields and aggregate extents are intentionally excluded from equality. The test passes.
- Release build `fps_depth_bounds_build_log.txt` succeeded; candidate SHA-256 `0C902A7C703D641FB6A2F8F39EA67D75DFB632B21ECC79B438A26C55426D6C25`. The planned clean comparison could not start because another project's XEMU (PID 11676, started 21:15:04) was active. It was left untouched; visual qualification proceeds separately and timing under contention cannot establish an FPS gain.
- `20260914_211605-fps-depth-bounds-4p-visual` completed 100 seconds, five good polls, no fatal reports. All three native captures were inspected chronologically: 30 seconds loading, 64 seconds four weapon/HUD/world views with primary-fire flashes and the yellow forcefield, 98 seconds open world views and a visible character in P3. The other emulator overlapped the run and exited before its end. Its 30.61 FPS aggregate and physics/host ratio 0.959 are not accepted as clean performance/simulation qualification.
- With no other XEMU present before, during observed inventories, or at the end, `20260914_211823-fps-depth-bounds-4p-clean` completed 120 seconds, seven good polls, no fatal reports. All native captures (37/70/102 seconds) were inspected in order: four world/HUD/weapon views, correct lit geometry, active movement/fire, a visible character in P1 at 102 seconds. Several views temporarily face close walls. This establishes the saved content, not exhaustive flicker/animation or audible-output coverage.
- Full clean sample: **40.887–40.892 host FPS** over 81.286–81.298 sampled seconds; game/host 1.00022–1.00036 and physics/host 0.99732–0.99746. The first six polls provide a closer duration match to the pre-change full-width baseline: **41.228–41.235 FPS** over 64.993–65.004 seconds versus baseline **40.712–40.721** over 64.806–64.821 seconds. The new matched sample has game/host 0.99945–0.99962 and physics/host 0.99594–0.99611; report the small accumulated physics lag rather than claiming exact equality.
- Inclusive frame-weighted world cost was 13.266 ms/frame after versus 14.522 before; model/things 6.362 versus 7.193; geometry 5.469 versus 5.921; submission 4.258 versus 4.152; simulation 7.381 versus 6.701. Different randomized combat and differing counter-window counts prevent an isolated speedup claim. The small net FPS change is not a reliable whole-game gain, although less world-rendering work is observed. Consistent 60 FPS remains unproven.
- The matched after-sample is saved in the clean run's `depth_bounds_comparison_sample.json`; the pre-change clean sample is in `pillarbox_4p_comparison.json`. The test-owned emulator exited normally. The verified source change is retained, the normal runtime install remains unchanged, and no routing work was performed.
